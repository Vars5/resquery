%h1 Groups#index
%p Find me in app/views/groups/index.html.haml

- if can? :manage, :all
	=link_to "New Group", new_group_path


%h3 Your Groups
- @groups.each do |group|
	%h5= link_to "#{group.name}", group 
	= simple_format(truncate((group.description),length: 160))
	%br

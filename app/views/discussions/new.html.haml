.discussion
	= simple_form_for [@group, @discussion] do |f|
		= f.input :title
		= f.input :description
		= f.input :user_id, as: :hidden, :input_html => { :value => current_user.id }
		= f.button :submit